<%= form_for @usuario do |f| %>
<% if @usuario.errors.any? %>
		<div class = "error_messages">
			<h2><%= pluralize(@usuario.errors.count, "error") %> impedem esse usuario de ser salvo:</h2>
			<ul>
				<% @usuario.errors.full_messages.each do |msg| %>
				<li> <%=msg%> </li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<div class="field">
		<%= f.label :username %><br />
		<% if @usuario.username.blank? then %>
		<%= f.text_field :username, :required => true %>
		<% else %>
			<%= f.text_field :username, :readonly => true, :required => true %>
		<% end %>
	</div>
	<div class="field">
		<%= f.label :nome %><br />
		<%= f.text_field :firstname %>
	</div>

	<div class="field">
		<%= f.label :sobrenome %><br />
		<%= f.text_field  :lastname %>
	</div>
	
	<div class="field">
		<%= f.label :senha %><br />
		<%= f.password_field  :password, :required => true  %>
	</div>

	
	<div class="actions">
		<%= f.submit :class => "btn btn-large btn-block btn-primary", :value => "Salvar" %>
	</div>


<% end %>	